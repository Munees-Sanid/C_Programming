#include <stdio.h>
#include <stdlib.h>

int main()
{
    float r,area;
    printf("enter the radius of the circle\n");
    scanf("%f",&r);
    area=3.14*r*r;
    printf("the area of the circle is %0.2f",area);
}
